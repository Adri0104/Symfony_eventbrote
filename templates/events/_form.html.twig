{{ form_start(form) }}
    {{ form_widget(form) }}
    <p>
        <input type="submit" class="btn btn-primary" value="{{ submitButtonText }}" formnovalidate> | <a href="{{ path('events.index') }}">Cancel</a>
    </p>
{{ form_end(form) }}
